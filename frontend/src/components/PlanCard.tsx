import { useTranslation } from 'react-i18next'
import type { Language, Plan } from '../types'
export default function PlanCard({plan,language,onSelect,index}:{plan:Plan;language:Language;onSelect:()=>void;index:number}){const{t}=useTranslation();const name=language==='id'?plan.name_id:plan.name_en;const prop=language==='id'?plan.proposition_id:plan.proposition_en;return <article className={`plan-card accent-${plan.accent}`} style={{'--delay':`${index*90}ms`} as React.CSSProperties}><div className="plan-card-top"><span className="plan-symbol">{plan.key==='easy_start'?'◒':plan.key==='fast_harvest'?'↗':'✣'}</span><span className="score-pill"><b>{plan.feasibility_score}</b>% {t('feasibility')}</span></div><h3>{name}</h3><p className="plan-proposition">{prop}</p><div className="plan-crop-stack">{plan.crops.slice(0,5).map(c=><span key={c.slug} title={language==='id'?c.name_id:c.name_en}>{c.slug.slice(0,2).toUpperCase()}</span>)}</div><div className="plan-metrics"><div><b>{plan.total_plants}</b><small>{t('plants')}</small></div><div><b>{plan.expected_first_harvest_days??'—'}</b><small>{t('days')}</small></div><div><b>{plan.weekly_care_minutes}</b><small>{t('minutes')}</small></div></div><div className="trade"><small>{t('tradeOff')}</small><p>{plan.trade_off}</p></div><button className="button primary full" onClick={onSelect}>{t('viewPlan')} <span>→</span></button></article>}
