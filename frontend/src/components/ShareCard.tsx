import { useTranslation } from 'react-i18next'
import type { Language, Plan } from '../types'
export default function ShareCard({plan,language,area}:{plan:Plan;language:Language;area:number}){const{t}=useTranslation();return <div className={`share-card accent-${plan.accent}`}><div className="share-brand"><img src="/groe-mark.svg"/><b>GROE</b></div><span className="share-kicker">{t('shareCardTitle')}</span><h3>{language==='id'?plan.name_id:plan.name_en}</h3><div className="share-big-number"><b>{area.toFixed(1)}</b><span>m²</span></div><div className="share-stats"><div><b>{plan.crop_profile_count}</b><small>crop profiles</small></div><div><b>{plan.total_plants}</b><small>{t('plants')}</small></div><div><b>{plan.expected_first_harvest_days??'—'}</b><small>{t('days')}</small></div></div><div className="mini-layout">{plan.layout.placements.slice(0,18).map((p,i)=><i key={p.placement_id} style={{left:`${8+(p.x_m/Math.max(1,Math.max(...plan.layout.plot_boundary.map(x=>x[0]))))*82}%`,top:`${8+(p.y_m/Math.max(1,Math.max(...plan.layout.plot_boundary.map(x=>x[1]))))*70}%`,transform:`scale(${.8+(i%3)*.15})`}}/>)}</div><p>{t('shareCardFooter')}</p></div>}
